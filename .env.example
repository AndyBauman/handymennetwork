PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
PUBLIC_SITE_URL=https://handymennetwork.com

# Geo/bot blocking lives in the Vercel WAF, not in app code — `output: "static"`
# means the adapter never emits edge middleware. Apply with `npm run firewall`
# (config in scripts/firewall.mjs); inspect with `npm run firewall:status`.
